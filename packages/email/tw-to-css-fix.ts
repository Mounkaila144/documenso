// Utiliser la méthode recommandée pour importer un module CommonJS dans un contexte ESM
import pkg from 'tw-to-css';

// Exporter l'objet approprié en utilisant la syntaxe de déstructuration recommandée
export const { tailwindToCSS } = pkg; 